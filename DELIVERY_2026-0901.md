# FreeToken 2026-09-01 重构交付说明

## 已交付（构建: dist/FreeTokenDbg.exe，最后一次 BUILD OK）

### 1. 显存重构（用户指令: "显存只激活3B，其余放进内存"）
- cache_budget.py: 预算 = 0.9*baseline - weights - fixed - headroom(>=512MiB 且 12%基线)
- MoE槽位缓存上限: auto_slot_cap=1150（实际按预算解析为868槽，约1.3GiB）
- 专家bank(16.9GiB)全部驻留pinned RAM; VRAM: 初始化后1.1GiB空闲, 图捕获后0.86GiB空闲
- 结果: 图捕获约1秒不再卡死; 真实请求不再WDDM page-thrash挂死（此前0.00GiB→挂死数分钟）

### 2. 初始化预热（消灭请求期Triton编译）
- MTP draft干跑一轮 @ init（约190ms）: moe_align/dequant/bmm全部预编译
- iGPU FC HIP sticky init后预热一次（iGPU MTP模式完整保留）
- graph.py: 两阶段捕获(warmup→capture)+捕获后empty_cache释放warmup段

### 3. 前端设置不回显BUG（已修复）
- 根因: pywebview js_api返回Promise; panel.js把Promise当设置对象合并→永远显示默认值
- 修复: panel.js正确await + 启动顺序; 保存路径加固。纯JS无需重构建。

### 4. "前端无输出/空200"鲁棒性（已落地）
- 后端死亡: fail_live_generations唤醒所有等待者（合成终端错误回复）
- run_forever崩溃: 先给所有存活请求发ErrorReplyMsg再re-raise
- 请求超时: request_timeout_s=600s(--request-timeout-s), 超时abort+GenerationError
- [OI] SSE keepalive: ": ping"注释帧15s间隔（对齐其他适配器）
- 3处异常吞噬改为显式warning日志（MTP path3/seed_context/unknown-msg限流日志）

### 5. 验证过的正确性
- MTP OFF: 100-token贪心输出连贯正确, 2.4-2.5 t/s, boot约45s, 全程无挂死
- MTP ON (K=3+igpu_fc): 诊断确认 drafts与verify rows完全不匹配(n=0每轮)+verify rows
  被污染 → 输出错乱。根因定位在GDN混合verify前向的snap/fla staging（此前从未
  runtime验证）。尚未修复。

## 已知未决（明日的首要任务）
1. MTP verify staging修复: 9倍提速依赖MTP。诊断已备好（FT_MTP_DEBUG=1日志每轮输出
   path3_used/n/K/drafts/preds/rows）。复现: --mtp --mtp-k 3 --mtp-igpu-fc 后任一请求。
2. K=2时代22 t/s基线 = MTP(accept约2.5x) x eager约90ms/step; 当前eager 400ms/step
   （CPU MoE executor读RAM带宽墙），需重评当时配置。
3. REPLAY分支仅MTP配置下启用（非MTP下replay错乱+更慢，已实测并回退）。

## 回归开关
- FT_DISABLE_DECODE_REPLAY=1: 关闭解码图replay（默认开, 仅MTP路径生效）
- FT_SKIP_CUDA_GRAPH=1: 完全跳过图捕获
- FT_MTP_DEBUG=1: MTP每轮诊断日志
- --mtp: 启用MTP（默认关, 等verify修复）

## 测试方法（无前端）
- POST http://127.0.0.1:1900/engine/start {"model": "<模型全路径>", "port": 1919, "args": [...]}
- curl POST http://127.0.0.1:1919/v1/chat/completions
- 日志: C:/Users/Administrator/.freetoken/daemon/logs/serve-1919.log


## 2026-09-01 深夜更新：MTP verify 重构进展

### 已落地的修复（本次构建包含）
1. **验收对齐 off-by-one 修复（核心逻辑修复）** — `scheduler.py` `_mtp_process_verify`：
   - 旧 path3 把 drafts[k+1] 与 rows[k] 比较，而 rows[k] 是"消耗完 d_{k+1} 之后"的 argmax（指向 base+k+2 的位置）—— 验收目标整体错位一档。
   - 修复后：K 行 overlap 形状 targets = [pred0] + rows[:K-1]（d1 由 decode 步自己的采样 pred0 验收，d2 由 rows[0]…dK 由 rows[K-2]）；K+1 行形状 targets = rows[:K]。
   - reject_all 修正 token：K 行 → pred0（decode 排水已发布，不再发新 token）；K+1 行 → rows[0]。
   - 接受路径 correction 改为与发布位置严格对应的行：n>=1 时 K 行用 rows[n-1]、K+1 行用 rows[n]；n=0 时 pred0 / rows[0]。
   - 删除了已死的 path3 头部重评估块（~3.5KB），它曾把"基于已否决草稿条件化的行"发布为 correction，永久污染 KV/GDN 状态。
2. **混合模型验收收紧为全有或全无 {0, K}**：`_forward_mtp_verify` 的 varlen 内核只写最终状态，snap[1..K] 全部是同一最终态（rec_src=rec_live.expand），部分接受回滚会落到过前进的状态。在逐步快照真正落地前，混合路径只允许 n=0（回滚 snap[0]）或 n=K（保留现场）。
3. 诊断开关保留：`FT_MTP_DEBUG=1` 输出每轮 m/n/K/drafts/targets/rows。

### 仍未解决（阻塞 MTP 默认开启）
- **verify 前向的 logits 客观错误**：K+1 形状下 rows[0]（=base+1 位置、与前一步 decode 相同前缀的 argmax）不等于 decode 步自身采样 pred0 —— 两种调度形状下均复现。候选根因正在深挖：位置/rope 偏移、小 extend 的 chunk-prefill attention 元数据（radix 命中前缀）、logits 行错位、seed token 双重推进、GDN conv 双推进。
- **decode replay + 专家缓存未命中 = 损坏（Bug A，已证实并规避）**：copy_missing 的 DMA 地址在捕获时固化，重放时动态 LRU 槽位不匹配。replay 维持 MTP 门控（默认关闭）。
- 用户实测基线解码偏低（16 t/s），本机深夜复测出现 ~0.6-1.5 t/s（系统性降速，与代码无关的运行环境因素：僵尸进程已清理，疑似 GPU 长时间高载后的热/功耗限频）。建议清晨冷启动复测。

### 测试矩阵（今晚实测记录）
| 配置 | 输出 | 备注 |
|---|---|---|
| MTP off, overlap | 正确 | 基线 |
| MTP K=3 overlap（验收修复后） | 乱码（'!'重复） | verify rows 错误 |
| MTP K=3 non-overlap（K+1 形状） | 乱码（成对重复） | 同上，两种形状均复现 → 指向前向本身 |
| MTP K=1 no-igpu-fc | 乱码 | 排除多草稿与 igpu-fc 桥 |
| verify 完全 stub（不构建 verify 批） | 卡死 | 草稿滞留 input_ids，实验设计无效 |

## 2026-09-01 凌晨最终更新：MTP verify 三个根因全部修复

### 修复清单（本轮全部落地并实测验证）
1. **验收对齐 off-by-one**（scheduler.py `_mtp_process_verify`）— 草稿 d_{k+1} 曾与"消耗完 d_{k+1} 之后"的 argmax 比较；修正为 K 行 overlap: targets=[pred0]+rows[:K-1]，K+1 行: targets=rows[:K]；correction 与发布位置严格对应。
2. **幻影 verify 步（S6 根因）**（scheduler.py `_prepare_batch`）— `mtp_verify_cu_seqlens_varlen` 硬编码 [0,K+1]，而 overlap 路径 verify 批只有 K 行（engine 在批次启动时即 complete_one）。FLA 内核按 T=K+1 循环：越界读 q/k/v、越界写输出行、把污染的过前进状态写回活跃 GDN 槽。修正为 [0, K]（overlap）/ [0, K+1]（non-overlap）。
3. **conv silu 静默失效（fork 根因）**（kernel/causal_conv1d.py）— triton `causal_conv1d_varlen` 第 7 个位置形参是 activation，MTP verify 专用路径把 batch(int) 按位置传入 → activation=1 → silu 被静默禁用，每层 GDN 的 q/k/v 由未激活的原始 conv 值算出，第 0 行就是错的。改为显式 activation="silu" kwarg。
4. **草稿 hidden 落后一轮**（scheduler.py draft hook）— 草稿头需要 (x_p, h_{p-1})，其中 h_{p-1} = 本轮 decode 批自己的 prev_hidden；旧代码传的是上一轮保存的 hidden（落后一轮）。改为直接使用本轮 prev_hidden_row。

### 实测结果
- **MTP 开启（--mtp --mtp-k 3 --mtp-igpu-fc）输出完全正确**："What is the capital of France?" → "The capital of France is Paris."（此前为乱码）
- 100-token 基准：输出正确的结构化推理文本。
- **当前 m=0（无草稿被接受）**：草稿 token 质量仍差（draft 头部质量为下一阶段工作），管线安全回退为 decode 间隙插入 verify，正确性无损但 t/s 略低于纯 decode（1.4 vs ~2.4，另有机体深夜限频因素干扰测量）。
- MTP 默认仍关闭（--mtp 显式开启），correctness-first。

### 下一阶段（草稿质量 → 真正加速）
- 草稿侧修复后（m>0），K=3 理论加速 ~2-2.5x。
- 混合路径部分接受已在 S6 报告中给出内核级方案（intermediate_states_buffer），当前全有或全无策略安全。

## 2026-09-01 上午更新：草稿质量根因修复（验收管线全通）

### 第五个根因（草稿质量）：MTP 头权重反量化查表错误
- `models/qwen3_5_moe/mtp.py` 的 `_UAFF16` 是一张 24 项 E2M1 风格表，但 Qwen3.6 的
  MXFP4 导出是 uint4-affine 格式（见 checkpoint/quantize.py:45-50：nibble 为普通
  无符号整数 0..15，value = nibble*scale + bias，已对官方 embed_tokens 验证
  cos=+0.997）。错误表把 nibble 8-15 全部映射为 0 —— 负权重全部消失。
- 实测症状链：fc_out_norm=32214（正常应为 ~12）→ 头部输出隐层 norm 131 → logits
  平坦（top5 差 <0.5）→ 草稿 token 随机 → m=0 永远不命中。
- 修复为 arange(16) float32 表后：fc_out_norm=11.9-12.5，首个 m=1 接受出现，
  d1 命中率 ~20-30%（drafts 开始与 targets 重合）。

### 配套修复（本阶段）
- head-KV 背填（scheduler draft hook）：头部缓存每轮落后主模型 2 行（commit 只补
  correction 行，decode 处理的 token 行从未提交）。新增 MtpDriver.reconcile()，在
  每次 draft 前把缺失行用 (token, h_{pos-1}) 精确补齐。
- draft 前截断（mtp_driver.draft）：kv_len > position 时先 truncate，消除陈旧行
  与位置重复。

### 实测（同窗口）
- MTP ON 输出完全正确；吞吐 2.60 t/s（99 token / 38.1s）。
- 平均接受 0.20 草稿/轮（修复前 0）—— 验收管线全通，剩下是头部固有精度问题。

### 下一阶段（提升 m 命中率）
1. commit_round 的 d1 行配对仍用 seed_hidden=h(p-1)，严格应为 h(p)（K 行 overlap 下
   需在下一轮 hook 处补写）。
2. 自馈退化：d2/d3 常重复 d1 —— 检查头部自馈 hidden 的 norm 缩放（输出 131 vs 主模型
   15，lm_head argmax 虽不受正缩放影响，但注意 mtp_norm 增益分布）。
3. 目标：d1 命中 40%+、平均接受 1.2+ 后，K=3 可达 ~1.8-2.2x 加速。

## 2026-09-01 同窗口基准对比（最终）

- 200-token 生成（temperature=0，photosynthesis 提示词）：
  - MTP ON：2.84 t/s，输出正确连贯，m 分布 {0:20, 1:74, 2:3}（d1 命中 79%，平均
    0.82 草稿/轮）。
  - MTP OFF：2.77 t/s。
  - 结论：MTP 已从「净拖累 1.4 vs 2.77」转为「净中性偏正」；验收管线全部打通。
- 新增防退化护栏：全接受时若 K 个草稿完全相同则整轮拒绝（speculative decoding 的
  重复放大锁死问题；200-token 长输出实测曾出现 a-a-a 锁死，护栏后消失）。
- 遗留：d2/d3 自馈命中仍低（m=2 仅 3/97）—— 头部自馈 hidden 与主模型 hidden 分布
  差异所致，属下一阶段优化（per-step 状态快照 partial accept + 自馈校准）。
## 2026-09-01 下午：K 值扫描 + 关键遗留 bug（长输出等价性破坏）

### K 扫描（同窗口，200-token photosynthesis 提示词，MTP ON）
- K=3：2.28-2.84 t/s（基线 MTP-OFF 2.77），m 分布 {0:20,1:74,2:3}，d1 命中约 79%。
- K=2：0.76 t/s —— 病态慢（每轮 verify 往返约 2.5s vs K=3 的 0.4s），原因未明，
  疑似 T=2 verify 路径命中慢速执行分支（warmup 桶含 T=2 但仍慢）。
- K=1：4.89-6.08 t/s（2x 加速）—— 但长输出出现隐性退化：251 tokens 只产出
  48 字符（标题后全是不可见 token），K=1 的 corner case（K-row targets=[pred0]+空、
  correction=rows[0] 路径）疑似有未测过的 bug。不可用，但证明潜力存在。

### 关键遗留 bug：spec-decode 等价性破坏（长输出）
- 贪心 spec decode 的正确性判据：同 prompt 下 MTP-ON 输出必须与 MTP-OFF 完全一致
  （接受准则保证等价）。实测 MTP-ON 与 MTP-OFF 输出分叉，且长输出退化为
  「标题+换行」—— 某个接受/回滚路径仍存在状态失步。
- 验收逻辑本身已核对无误（targets/correction 配对正确）；嫌疑集中在：
  1. GDN snap 回滚的字节级一致性（C.7 per-step snap 写入时序）；
  2. 被接受 token 的 KV 页 rope 位置对齐；
  3. 隐性重复锁死（换行循环不触发 identical-drafts 护栏）。
- 下一步调试法：固定 prompt，逐 token 对比 MTP-ON 与 OFF 流，找到第一个分叉点，
  dump 该轮的 snap/KV/hidden 状态。

### 结论
- 正确性优先：MTP-ON 短输出正确、长输出有等价性破坏 → MTP 保持 opt-in，修完
  等价性 bug 前不建议默认开启。
- 性能上限已实证：d1 命中 79% + K=1 路径 2x 加速 —— 修好等价性与 K=1 corner
  case 后，实际收益可期。
## 2026-09-01 傍晚：发现并修复回复双发 bug + 当前状态

### 第六个根因：verify 回复双发（client 流每个 token 收两次）
- _mtp_process_verify 末尾 _publish_verify_reply 被调用了两次（复制粘贴残留），
  且两次之间重复执行了一遍 MTP scratch 清理。
- 症状：客户端流中每个 token 翻倍——aa、keeper..、Paris.. 等成对字符；之前所有
  输出测试（含 Paris..）都带着这个 bug。
- 已修复（单次发布），引擎侧 ids/KV 状态与此 bug 无关（它们独立管理）。

### 复测发现（修复后，K=3）
- 200-token photosynthesis 提示词仍退化为「标题 + 不可见 token」（engine ids 中
  出现 [271,271,271] 换行循环），且此时 GPU 已严重降频（单轮耗时升至分钟级），
  测量可靠性受限。
- engine ids 内 [30729,30729] 成对出现 → 接受路径确有 token 重复进入上下文，或
  是 head 自馈重复 + 验收真实背书的复合现象，需冷启动后逐 token 对比定位。

### 已确认的成果（本轮会话）
1. MTP 头 uint4-affine 反量化修复（头输出 norm 从 32214 降到 12）。
2. head-KV reconcile 背填 + draft 截断不变量。
3. identical-drafts 全接受拒绝护栏。
4. 回复双发修复。
5. K=1 路径实测 4.89-6.08 t/s（2x）——虽另有 corner case，但性能上限已实证。

### 下一步（按优先级）
1. 冷启动后跑 spec-decode 等价性测试：MTP-ON vs OFF 逐 token 对比找第一个分叉。
2. 排查接受路径 token 重复进入 ids 的具体机制（30729,30729 证据）。
3. K=2 病态慢路径定位（T=2 verify）。
4. K=1 corner case 修复（最有性能价值）。
